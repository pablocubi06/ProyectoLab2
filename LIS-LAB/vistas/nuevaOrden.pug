html(lang="en")
  head
    meta(charset="UTF-8")
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js")
    link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css", rel="stylesheet")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")    
    title Document
  body
    .row
      .col-2
      .col-8
        figure(class="text-center")
          blockquote.blockquote
            h3(class="blockquote-footer") Agregar nueva ORDEN
        form(class="row g-2")
          .col-4
            label(for="inputAddress", class="form-label") Fecha de ingreso
            input(type="date", class="form-control", id="fechaInput")
          .col-4
            label(for="inputAddress", class="form-label") Fecha de entrega
            input(type="date", class="form-control", id="inputAddress", disabled)
          .col-4
            label(for="inputAddress", class="form-label") Estado
            input(type="text", class="form-control", name="", disabled)
          .col-2
            label(for="inputAddress", class="form-label") DNI paciente
            input(type="text", class="form-control", name="",value=persona.dni, disabled)
          .col-5
            label(for="inputAddress", class="form-label") Apellido del paciente
            input(type="text", class="form-control", id="inputAddress",value=persona.apellido, disabled)
          .col-5
            label(for="inputAddress", class="form-label") Nombre del paciente
            input(type="text", class="form-control", id="inputAddress",value=persona.nombre, disabled)
          .col-md-6
            label(for="inputEmail4", class="form-label") Dias de demora
            input(type="number", class="form-control", id="inputEmail4")
          .col-12
            button(type="submit", class="btn btn-info") Generar orden

        button(type="button", class="btn btn-secondary", data-bs-toggle="modal", data-bs-target="#staticBackdrop") Agregar examenes

        .modal.fade(id="staticBackdrop", data-bs-backdrop="static", data-bs-keyboard="false", tabindex="-1", aria-labelledby="staticBackdropLabel", aria-hidden="true")
          .modal-dialog
            .modal-content
              .modal-header
                h5.modal-title(id="staticBackdropLabel") Cargar exámen a la orden generada
                button(type="button", class="btn-close", data-bs-dismiss="modal", aria-label="Close")
              .modal-body
                form(class="row g-2")
                  .col-7
                    label(for="inputAddress", class="form-label") Código orden
                    input(type="text", class="form-control", name="", disabled)
                  .col-6
                    label(for="inputAddress", class="form-label") Código examen
                    input(type="text", class="form-control", name="", disabled)
                  .col-6
                    label(for="inputAddress", class="form-label") Tipo de examen
                    select(class="form-select", name="tipo_examen", required)
                      option(selected disabled value="") Selecciona
                      option(value="") Administrador
                  .col-6
                    label(for="inputAddress", class="form-label") Determinación
                    select(class="form-select", name="tipo_examen", required)
                      option(selected disabled value="") Selecciona
                      option(value="") Administrador
                  .col-6
                    label(for="inputAddress", class="form-label") Valor de referencia adecuado
                    select(class="form-select", name="tipo_examen", required)
                      option(selected disabled value="") Selecciona
                      option(value="") Administrador
                  .mb-3
                    label(for="validationTextarea", class="form-label") Resultado
                    textarea(class="form-control", id="validationTextarea", disabled)
                    .invalid-feedback
                      Resultado
                  .col-12
                    button(type="button")
    script(src="/script/fechaActual.js") 